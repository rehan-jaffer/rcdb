%h3 Consumer Reports

= render reports.approved
= simple_form_for(@report) do |f|
  = f.input :title
  = f.input :report
  = f.hidden_field :drug_id, :value => drug_id
  = f.submit :class => "btn btn-primary"

